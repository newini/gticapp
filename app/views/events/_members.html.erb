<div class="pagination pagination-centered"><%= will_paginate @members, renderer: BootstrapPagination::Rails %></div>
<table class="table table-hover table-bordered table-condensed">
  <thead>
    <tr>
      <th>ID</th>
      <th>氏名</th>
      <th>Facebook名</th>
      <th>所属</th>
      <th>招待</th>
    </tr>
  </thead>
  <% @members.each do |member| %>
    <tr>
      <td><%= member.id %></td>
      <td><%= link_to member.name, member_path(member) %></td>
      <td><%= member.facebook_name %></td>
      <td><%= member.affiliation %></td>
      <td><%= link_to check_invite_flg(member), 
            change_connection_event_path(
              :invited_member_id => member.id), 
              :method => :post,
              :class => "btn btn-small btn-primary btn-block"%></td>

    </tr>
  <% end %>
</table>
<div class="pagination pagination-centered"><%= will_paginate @members, renderer: BootstrapPagination::Rails %></div>
