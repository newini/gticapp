<div class="row">
  <aside class="span5">
    <section>
      <%= render 'event_form' %>
      <%= link_to '削除', @event, value:@event.id, method: :delete, class: 'btn btn-large btn-block btn-warning', data: {confirm: "削除してもよろしいですか？"} %>
    </section>
    <section class="well well-small">
      <%= render 'import_participants' %>
    </section>
  </aside>
  <div class="span7">
    <%= render 'participants' %>
  </div>
</div>

