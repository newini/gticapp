<h2>一斉送信文面作成</h2>

<strong>注意：本文冒頭は常に「〇〇様」が自動的に入る</strong>

<hr>

<%= form_for [@event, @broadcast], :html => { :class => 'form-horizontal center' } do |f| %>

  <div class="control-group">
    <%= f.label :subject, 'メールタイトル' %>
    <%= f.text_field :subject, class: "form-control" %>
  </div>

  <div class="control-group">
    <%= f.label :body, 'メール本文' %>
    <%= f.text_area :body, class: "form-control", rows: 1, readonly: true,
      value: "【メール送信時は、メール本文の1行目に、〇〇様と個別名が自動的に挿入されます。】" %>
    <%= f.text_area :body, class: "form-control", rows: 20 %>
  </div>

  <br>

  <div>
    <%= f.submit "保存", class: "btn btn-primary btn-block center-block", style: 'width:20%' %>
  </div>
<% end %>
<p>作成したら、保存して次に進む</p>
