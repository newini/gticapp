<div class="row">
  <div class="col-md-2">
    <%= render'side' %>
  </div>

  <div class="col">
    <h2>Contact List</h2>

    <table class="table table-bordered table-striped table-sm" style="font-size: 13px;">
      <thead>
        <tr>
          <th width="10%">登録した日にち</th>
          <th width="15%">Name</th>
          <th width="15%">属性</th>
          <th>所属</th>
          <th>肩書</th>
          <th>Email</th>
          <th>Subject</th>
          <th>Body</th>
          <th>is read?</th>
          <th>is responsed?</th>
          <th style="width: 5%;"></th>
        </tr>
      </thead>

      <tbody>
        <% @contacts.each do |contact| %>
          <tr>
            <td><%= contact.created_at.strftime("%Y-%m-%d") %></td>
            <td><%= contact.name %></td>
            <td><%= Category.find(contact.category_id).name if contact.category_id %></td>
            <td><%= contact.affiliation.truncate(10) %></td>
            <td><%= contact.title.truncate(10) %></td>
            <td><%= contact.email %></td>
            <td><%= contact.subject.truncate(10) %></td>
            <td><%= contact.body.truncate(20) %></td>
            <td><%= get_check_icon(contact.is_read) %></td>
            <td><%= get_check_icon(contact.is_response_completed) %></td>
            <td><%= link_to "詳細", contact_path(contact), class: 'btn btn-info btn-sm' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>
